[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "math-to-manim-codex"
version = "0.1.0"
description = "Codex/OpenAI Agents SDK pipeline for prompt-to-Manim educational animation."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Math-To-Manim contributors" }]
dependencies = [
  "openai-agents>=0.3.3",
  "pydantic>=1.10,<3",
  "pyyaml>=6",
]

[project.optional-dependencies]
dev = [
  "pytest>=8",
  "pytest-cov>=4",
  "ruff>=0.4",
]
render = [
  "manim>=0.19",
]
web = [
  "fastapi>=0.110",
  "gradio>=4",
  "httpx>=0.27",
  "uvicorn>=0.29",
]

[project.scripts]
math-to-manim = "math_to_manim.cli:main"
m2m2 = "math_to_manim.cli:main"

[tool.setuptools.packages.find]
include = ["math_to_manim*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"

[tool.ruff]
target-version = "py310"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = ["E501", "N812"]  # allow long lines (handled by formatter); allow lowercase imported as

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["N802", "N803"]

[tool.ruff.format]
quote-style = "double"

[tool.coverage.run]
source = ["math_to_manim"]
omit = ["*/tests/*", "*/__pycache__/*"]

[tool.coverage.report]
precision = 1
skip_covered = false
skip_empty = true
sort = "Cover"
exclude_lines = [
  "pragma: no cover",
  "if __name__ == .__main__.:",
  "raise NotImplementedError",
  "if TYPE_CHECKING:",
]
